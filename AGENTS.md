# WorthDelta release checklist

For every user-facing feature, fix, or design change:

1. Bump `APP_VERSION` in `src/App.tsx` before committing.
2. Run the production build and lint checks.
3. Include the version bump in the same commit as the user-facing change whenever possible.
